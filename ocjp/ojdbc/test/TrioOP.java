package test;

public class TrioOP {
    public static void main(String[] args) {
        int x = -10;
        int y = 50;
        int absX = x>=0 ? x : -x;
        System.out.println(absX);

        char grade = y >= 90 ? 'A' : y>=80 ? 'B' : 'C';
        
    }
}
